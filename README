Installation:

    git clone git://github.com/amphi/dotvim.git ~/.vim
    cd ~/.vim
    git submodule init
    git submodule sync
    git submodule update

Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc
